{
  "name": "app-ads-api",
  "version": "0.1.0",
  "private": true,
  "description": "GPSA Scoreboard Ads — Ads API (Fastify). The only credentialed component.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "eta": "^3.5.0",
    "fastify": "^5.2.1",
    "minio": "^8.0.4",
    "nodemailer": "^6.10.0",
    "sharp": "^0.33.5"
  }
}
