// Runtime configuration for the team-view browser app. Rendered from this template at
// container start by docker-entrypoint.sh (envsubst) into /usr/share/nginx/html/config.js.
// Only PUBLIC values live here — never a secret (DESIGN.md §3 inv 3).
window.GPSA_TEAM_CONFIG = {
  adsFormUrl: "${ADS_FORM_URL}",
};
